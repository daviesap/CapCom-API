{
  "name": "flair-pdf-project",
  "private": true,
  "scripts": {
    "deploy:frontend": "cd frontend && npm run build && cd .. && firebase deploy --only hosting --project flair-pdf-generator",
    "deploy:functions": "firebase deploy --only functions --project flair-pdf-generator",
    "deploy:rules": "firebase deploy --only firestore:rules --project flair-pdf-generator",
    "deploy:all": "npm run deploy:frontend && npm run deploy:functions && npm run deploy:rules",
    "preversion": "echo \"❌ Don't run 'npm version' in the repo root. Use 'npm run version:functions -- <patch|minor|major>'\" && exit 1",
    "version:functions": "npm --prefix functions version",
    "emulate:functions": "GLIDE_LOGS_APP='rIeUzfTQpgK4ComVOanX' GLIDE_LOGS_TABLE='native-table-EXf6OrHFQHS6x7kVCQ10' LOCAL_API_KEY='Sausages2025!!' firebase emulators:start --only functions",
    "cf:login": "wrangler login",
    "cf:deploy": "wrangler deploy --config infra/cloudflare/wrangler.toml",
    "cf:preview": "wrangler dev --config infra/cloudflare/wrangler.toml"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "autoprefixer": "^10.4.21",
    "eslint": "^9.26.0",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^16.1.0",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.7",
    "wrangler": "^4.33.1"
  },
  "dependencies": {
    "@glideapps/tables": "^1.0.21",
    "@tailwindcss/vite": "^4.1.7",
    "date-fns": "^4.1.0",
    "react-json-view": "^1.21.3"
  }
}
