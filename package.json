{
  "name": "flair-pdf-project",
  "version": "1.0.0",
  "private": true,
"scripts": {
  "deploy:frontend": "cd frontend && npm run build && firebase deploy --only hosting --project flair-pdf-generator",
  "deploy:pdfgen": "cd pdfGeneratorFunction && npm run deploy",
  "deploy:all": "npm run deploy:frontend && npm run deploy:pdfgen"
}
}